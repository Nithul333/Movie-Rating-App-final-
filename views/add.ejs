<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Add a New Movie</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <!-- Tailwind CSS CDN -->
  <script src="https://cdn.tailwindcss.com"></script>

  <!-- Google Font -->
  <link href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@300;600&display=swap" rel="stylesheet">

  <style>
    body {
      font-family: 'Open Sans', sans-serif;
      background: linear-gradient(to right, #0f2027, #203a43, #2c5364);
      color: #f1f1f1;
    }

    .fade-in {
      animation: fadeIn 0.6s ease-out;
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
  </style>
</head>
<body class="flex items-center justify-center min-h-screen px-4">
  <div class="w-full max-w-2xl bg-white/10 backdrop-blur-md p-8 rounded-xl shadow-lg fade-in">
    <h1 class="text-3xl font-bold text-center mb-6 text-white">🎬 Add a New Movie</h1>
    <div class="text-center mb-6">
      <a href="/" class="text-yellow-400 hover:underline">← Back to Home</a>
    </div>

    <form action="/add" method="post" class="space-y-5">
      <div>
        <label class="block mb-1 font-semibold">Movie Title</label>
        <input type="text" name="title" placeholder="Enter movie title" class="w-full px-4 py-2 rounded-md bg-white/20 text-white placeholder-gray-300 focus:outline-none focus:ring-2 focus:ring-yellow-400" required>
      </div>
      <div>
        <label class="block mb-1 font-semibold">Director Name</label>
        <input type="text" name="director" placeholder="Enter director's name" class="w-full px-4 py-2 rounded-md bg-white/20 text-white placeholder-gray-300 focus:outline-none focus:ring-2 focus:ring-yellow-400" required>
      </div>
      <div>
        <label class="block mb-1 font-semibold">Genre</label>
        <input type="text" name="genre" placeholder="Enter genre" class="w-full px-4 py-2 rounded-md bg-white/20 text-white placeholder-gray-300 focus:outline-none focus:ring-2 focus:ring-yellow-400" required>
      </div>
      <div>
        <label class="block mb-1 font-semibold">Release Year</label>
        <input type="number" name="releaseYear" placeholder="e.g. 2023" min="1900" max="2030" class="w-full px-4 py-2 rounded-md bg-white/20 text-white placeholder-gray-300 focus:outline-none focus:ring-2 focus:ring-yellow-400" required>
      </div>
      <div>
        <label class="block mb-1 font-semibold">Rating (1–5)</label>
        <input type="number" name="rating" min="1" max="5" class="w-full px-4 py-2 rounded-md bg-white/20 text-white placeholder-gray-300 focus:outline-none focus:ring-2 focus:ring-yellow-400" required>
      </div>
      <div>
        <label class="block mb-1 font-semibold">Review</label>
        <textarea name="review" placeholder="Write a short review" rows="4" class="w-full px-4 py-2 rounded-md bg-white/20 text-white placeholder-gray-300 focus:outline-none focus:ring-2 focus:ring-yellow-400" required></textarea>
      </div>
      <div>
        <label class="block mb-1 font-semibold">Thumbnail Image URL</label>
        <input type="text" name="thumbnailImage" placeholder="https://example.com/image.jpg" class="w-full px-4 py-2 rounded-md bg-white/20 text-white placeholder-gray-300 focus:outline-none focus:ring-2 focus:ring-yellow-400" required>
      </div>
      <div class="text-center">
        <button type="submit" class="mt-4 bg-yellow-400 hover:bg-yellow-300 text-gray-900 font-semibold px-6 py-2 rounded-full transition duration-300 shadow-md">
          ➕ Add Movie
        </button>
      </div>
    </form>
  </div>
</body>
</html>
